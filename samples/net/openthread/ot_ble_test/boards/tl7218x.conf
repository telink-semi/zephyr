# usb
CONFIG_USB_DEVICE_STACK=n
CONFIG_USB_DEVICE_INITIALIZE_AT_BOOT=n

# Telink relative thread features
CONFIG_NET_PKT_TXTIME=y
CONFIG_NET_PKT_TIMESTAMP=y
CONFIG_IEEE802154_TLX_MAC_FLASH=y

CONFIG_OPENTHREAD_JOINER=n
CONFIG_OPENTHREAD_COMMISSIONER=n
CONFIG_OPENTHREAD_COAP=n
CONFIG_OPENTHREAD_COAPS=n
CONFIG_OPENTHREAD_TCP_ENABLE=n
CONFIG_OPENTHREAD_DNS_CLIENT_OVER_TCP=n

# SMP: Match Telink demo: Unauthenticated_Pairing_with_Encryption
# = LE Security Mode 1 Level 2, Just Works, no MITM
# Disable SC-pairing-only (default y) to allow legacy pairing with encryption
# CONFIG_BT_SMP_SC_PAIR_ONLY=n
# CONFIG_BT_SMP_ENFORCE_MITM=n
# CONFIG_BT_SMP_ALLOW_UNAUTH_OVERWRITE=y
# CONFIG_BT_KEYS_OVERWRITE_OLDEST=y

# Bonding: persist pairing keys in flash so reconnects skip re-pairing
# CONFIG_BT_SETTINGS=y
# CONFIG_FLASH=y
# CONFIG_FLASH_MAP=y
# CONFIG_SETTINGS=y

# Channel Sounding (RAS Reflector)
# CONFIG_BT_CHANNEL_SOUNDING=y
# CONFIG_BT_TLX_CHANNEL_SOUNDING=y
# CONFIG_BT_GATT_DYNAMIC_DB=y
# CONFIG_BT_GATT_CLIENT=y

# BLE buffers for RAS data transfer (reduced for OT coexistence)
# CONFIG_BT_BUF_ACL_RX_SIZE=251
# CONFIG_BT_BUF_ACL_TX_SIZE=251
# CONFIG_BT_BUF_ACL_TX_COUNT=4
# CONFIG_BT_BUF_EVT_RX_COUNT=6
# CONFIG_BT_L2CAP_TX_BUF_COUNT=4
# CONFIG_BT_L2CAP_TX_MTU=247
# CONFIG_BT_ATT_TX_COUNT=4

# Stack sizes
# CONFIG_MAIN_STACK_SIZE=2048
# CONFIG_BT_RX_STACK_SIZE=2048
# CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=2048
# CONFIG_TL_BLE_CTRL_THREAD_STACK_SIZE=8192
